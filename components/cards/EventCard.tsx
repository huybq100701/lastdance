interface Props{
  id: string;
  currentUserId: string;
  title: string;
  location: string;
  eventTime: string;
  description: string;
  opponentId: string;
  createdAt: string;
}

function EventCard({
  id,
  currentUserId,
  title,
  location,
  eventTime,
  description,
  opponentId,
  createdAt,
}: Props) {
  return(
    
  )
}